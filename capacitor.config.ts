import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.myapp.doomwatch',
  appName: 'doomwatch',
  webDir: 'dist/doomsday-clock-app/browser'
};

export default config;
